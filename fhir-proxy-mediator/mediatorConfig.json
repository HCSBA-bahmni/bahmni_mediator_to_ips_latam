Below is the updated `mediatorConfig.json` for your FHIR Proxy Channel, changing the route `path` entries to `/` so that OpenHIM will forward the full request path correctly:

```json
{
  "urn": "urn:mediator:fhir-proxy",
  "version": "1.0.0",
  "name": "OpenMRS FHIR Proxy Mediator",
  "description": "Proxy universal de la API FHIR de OpenMRS vía OpenHIM",
  "defaultChannelConfig": [
    {
      "name": "FHIR Proxy Channel",
      "urlPattern": "^/proxy/fhir/.*$",
      "routes": [
        {
          "name": "FHIR Proxy Route",
          "host": "10.68.174.209",
          "path": "/",
          "port": "7000",
          "primary": true,
          "type": "http"
        }
      ],
      "allow": ["admin"],
      "methods": ["GET", "POST", "PUT", "PATCH", "DELETE"],
      "type": "http"
    },
    {
      "name": "FHIR Proxy Health Channel",
      "urlPattern": "^/proxy/health$",
      "routes": [
        {
          "name": "FHIR Proxy Health Route",
          "host": "10.68.174.209",
          "path": "/health",
          "port": "7000",
          "primary": true,
          "type": "http"
        }
      ],
      "allow": ["admin"],
      "methods": ["GET"],
      "type": "http"
    }
  ],
  "endpoints": [
    {
      "name": "fhir-proxy-endpoint",
      "path": "/proxy/fhir/*",
      "method": "*"
    },
    {
      "name": "health-endpoint",
      "path": "/proxy/health",
      "method": "get"
    }
  ],
  "heartbeatPath": "/proxy/health",
  "heartbeatInterval": 30000
}
```
